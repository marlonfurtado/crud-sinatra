box: ruby
# ruby:2.3.0

build:
  steps:
    - bundle-install
    - script:
        name: build
        code: bundle install

database:
  steps:
    - script:
        name: database
        code: bundle exec rake db:reset

test:
  steps:
    - script:
        name: test
        code: bundle exec rspec -fd
